rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /messages/{messageId} {
      allow read, create: if true;
      allow update, delete: if false; // Only allow reading and writing new messages, no tampering
    }
  }
}
